spring.application.name=Proyecto Ecommerce
port=8081

# Configuración de la base de datos
spring.datasource.url=jdbc:postgresql://localhost:5432/ecommerce
spring.datasource.username=postgres
spring.datasource.password=Asdf1474

# Configuración de JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

#desactivar la seguridad
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration

# Clave secreta fuerte, de al menos 256 bits (32 caracteres alfanuméricos)
jwt.secret=unaClaveMuySegura12345678901234567890

# Expiración del token en milisegundos (2 horas)
jwt.expiration=7200000